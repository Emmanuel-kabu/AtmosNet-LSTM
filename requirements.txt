# Core ML & Data
tensorflow>=2.12,<2.17
pandas>=2.0,<3.0
numpy>=1.24,<2.0
scikit-learn>=1.3,<2.0
scipy>=1.10

# Visualisation
matplotlib>=3.7
seaborn>=0.12

# API / Serving
fastapi>=0.100
uvicorn[standard]>=0.22
pydantic>=2.0
pydantic-settings>=2.0

# Configuration
python-dotenv>=1.0

# Database
sqlalchemy>=2.0
psycopg2-binary>=2.9

# Serialisation
joblib>=1.3

# ── Monitoring & Experiment Tracking ────────────────────────────────

# Weights & Biases — experiment tracking, weight monitoring
wandb>=0.16

# Evidently AI — data drift, concept drift, model performance
evidently>=0.4

# Prometheus — overall metrics, API latency, inference latency
prometheus-client>=0.17
prometheus-fastapi-instrumentator>=6.0
statsmodels  # Classical time-series models + diagnostics (ACF/PACF, ARIMA/ETS)
optuna  # Hyperparameter optimization (good for LSTM tuning)
# keras-tuner  # Keras-native hyperparameter tuning
tensorflow-addons  # Extra TF ops/losses/metrics; may require version pinning to match TF 2.8
pydantic  # Data validation & schemas (FastAPI already depends on it; pin explicitly if you use it directly)
httpx  # HTTP client (async-friendly); useful for pulling external weather/air-quality APIs
prometheus-fastapi-instrumentator  # Prometheus metrics middleware for FastAPI
pyarrow  # Parquet/Arrow IO for large time-series datasets

# Dev-only tools were moved to requirements-dev.txt